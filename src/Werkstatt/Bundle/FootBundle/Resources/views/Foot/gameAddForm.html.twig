<form action="{{ path('add_game') }}" method="post" {{ form_enctype(form) }}>
    <h3>Formulaire </h3>
    <div class="well">
        <div>    
            {{ form_row(form.date) }}
        </div>
        <div>
            <button type="submit">Ajouter</button>
        </div>
        {{ form_rest(form) }}
    <div>
</form>   